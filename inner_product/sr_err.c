#include "algo.h"
#include <stdio.h>
#include <math.h>
#include <string.h>

int main(int argc, char** argv)
{
    if (argc != 5)
    {
        printf("Usage: %s <vector size> <vector file> <result file> <repeat times>\n", argv[0]);
        return 1;
    }

    return 0;
} 